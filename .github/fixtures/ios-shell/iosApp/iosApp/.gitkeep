Present so the scaffold's Xcode-project detection has something to find.
CI compiles the generated Swift with swiftc against the real iOS SDK rather
than through xcodebuild, so no actual project file is needed.
